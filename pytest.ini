[pytest]
markers =
    unit: Unit tests for individual functions
    integration: Integration tests between components
    edge: Edge case tests

[coverage:run]
source = src/pip_audit_html
omit =
    */venv/*
    */tests/*
    */test_*

[coverage:report]
exclude_lines =
    pragma: no cover
    raise NotImplementedError
